<view class="container">
  <view class="header">
    <text class="title">事件详情</text>
    <button class="back-btn" bindtap="goBack">返回</button>
  </view>

  <view class="detail-card">
    <!-- 基本信息 -->
    <view class="basic-info">
      <view class="info-row">
        <text class="label">时间:</text>
        <text class="value">{{event.time}}</text>
      </view>
      <view class="info-row">
        <text class="label">国家:</text>
        <text class="value">{{event.country}}</text>
      </view>
      <view class="info-row">
        <text class="label">事件:</text>
        <text class="value event-name">{{event.name}}</text>
      </view>
      <view class="info-row">
        <text class="label">重要性:</text>
        <text class="value importance">{{importanceText}}</text>
      </view>
    </view>

    <!-- 数据对比 -->
    <view class="data-section">
      <text class="section-title">📊 数据对比</text>
      <view class="data-grid">
        <view class="data-box">
          <text class="box-label">预期</text>
          <text class="box-value forecast">{{event.forecast || '-'}}</text>
        </view>
        <view class="data-box">
          <text class="box-label">前值</text>
          <text class="box-value previous">{{event.previous || '-'}}</text>
        </view>
        <view class="data-box" wx:if="{{event.actual}}">
          <text class="box-label">实际</text>
          <text class="box-value actual">{{event.actual}}</text>
        </view>
      </view>
    </view>

    <!-- AI完整分析 -->
    <view class="ai-section">
      <text class="section-title">🤖 AI深度分析</text>
      <scroll-view class="ai-content" scroll-y>
        <text class="ai-text">{{event.ai_analysis}}</text>
      </scroll-view>
    </view>

    <!-- 操作提示 -->
    <view class="tips-section">
      <text class="tips-title">💡 交易提示</text>
      <text class="tips-text">建议结合技术分析和其他指标综合判断，单一事件不构成交易决策依据。</text>
    </view>
  </view>
</view>